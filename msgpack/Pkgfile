# Description: its like json, but fast and small
# URL: https://msgpack.org
# Maintainer: adam, adamvonbaron at outlook dot com
# Depends on: cmake doxygen

name=msgpack
version=3.3.0
release=1
source=(https://github.com/msgpack/msgpack-c/archive/cpp-$version.tar.gz)

build() {
    cd msgpack-c-cpp-$version
    cmake . -DCMAKE_INSTALL_PREFIX=/usr
    make
    make DESTDIR=$PKG install
}
